[project]
name = "automated-testing-framework-python"
version = "1.0.0"
description = "A beginner-friendly modular Python automated testing framework."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Shrish Kulkarni" }]
dependencies = ["colorama>=0.4.6"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]
